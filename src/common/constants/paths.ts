export enum Paths {
  // Public
  INDEX = "/",

  // Auth
  LOGIN = "/auth/login",
  REGISTER = "/auth/register",

  // Protected
  DASHBOARD = "/dashboard",
  OFFERS = "/offers",
  CANDIDATES = "/candidates",
  PIPELINE = "/pipeline",
  MAILING = "/mailing",
  STATS = "/stats",

  // Not found
  NOT_FOUND = "*",
}
